%p= link_to 'Добавить студента', [:new, :admin, :student], class: "btn btn-info"

%table.table.table-hover
  %thead
    %tr
      %th #
      %th Фамилия
      %th Имя
      %th Отчество
      %th Университет
      %th E-mail
      %th
  %tbody
    - @students.each do |student|
      %tr
        %td= student.id
        %td= student.last_name
        %td= student.first_name
        %td= student.middle_name
        %td= student&.university&.name
        %td= student.email
        %td
          = link_to [:edit, :admin, student], class: 'btn btn-warning', title: t(:edit) do
            = fa_icon "edit"
          = link_to [:admin, student], data: { method: :delete, confirm: "Вы уверены, что хотите продолжить?" }, class: 'btn btn-danger' do
            = fa_icon "recycle"
